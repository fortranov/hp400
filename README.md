# HP LaserJet Pro 400 Scanner Counter Control

Набор скриптов для управления счетчиком отсканированных изображений принтера **HP LaserJet Pro 400** через протокол **PJL (Printer Job Language)**.

Поддерживает как **сетевое** (Ethernet/Wi-Fi), так и **USB** подключение с автоматическим определением типа соединения.

## 🚀 Возможности

- ✅ Получение текущего значения счетчика отсканированных изображений
- ✅ Установка произвольного значения счетчика
- ✅ Сброс счетчика в 0
- ✅ Получение информации о принтере
- ✅ Поддержка различных PJL команд для максимальной совместимости
- ✅ Детальное логирование операций
- ✅ Обработка ошибок подключения

## 📋 Требования

- Python 3.6 или выше
- Принтер HP LaserJet Pro 400 в сети
- Сетевое подключение к принтеру

## 🔧 Установка

1. Скачайте скрипт:
```bash
git clone <repository-url>
cd printer
```

2. Убедитесь, что у вас установлен Python 3.6+:
```bash
python --version
```

3. Скрипт использует только стандартные библиотеки Python, дополнительная установка не требуется.

## 📁 Доступные скрипты

| Скрипт | Описание | Подключение | Чтение ответов | Зависимости |
|--------|----------|-------------|----------------|-------------|
| `hp_m425_scanner_counter.py` | **🎯 M425 MFP** - Специально для M425 PCL | USB + Сеть | 💾 MFP + WMI/CUPS | ❌ Нет |
| `hp_scanner_counter_system.py` | **🎯 СИСТЕМНЫЙ** - Только системные команды | USB + Сеть | 💾 Кэш + WMI/CUPS | ❌ Нет |
| `hp_scanner_counter_improved.py` | **🌟 УЛУЧШЕННЫЙ** - Исправляет проблему "Command sent" | USB + Сеть | ✅ Полный | ⚠️ pyusb |
| `hp_scanner_counter_auto.py` | Автоматически определяет тип подключения | USB + Сеть | ✅ Стандартный | ⚠️ pyusb |
| `hp_scanner_counter_usb.py` | Для USB подключения | USB | ⚠️ Ограниченный | ⚠️ pyusb |
| `hp_scanner_counter.py` | Для сетевого подключения | Ethernet/Wi-Fi | ✅ Полный | ❌ Нет |
| `test_connection_usb.py` | Диагностика USB | - | - | ⚠️ pyusb |
| `test_connection.py` | Диагностика сети | - | - | ❌ Нет |

## 💡 Использование

### 🎯 Для HP M425 MFP PCL (специализированная версия)
```bash
# РЕКОМЕНДУЕТСЯ для M425: Оптимизировано для MFP
python hp_m425_scanner_counter.py --list
python hp_m425_scanner_counter.py --get
python hp_m425_scanner_counter.py --set 1000

# GUI интерфейс для M425
run_m425_scanner_counter.bat
```

### 🎯 Системный метод (БЕЗ внешних зависимостей)
```bash
# Универсальный: Только системные команды, никаких библиотек
python hp_scanner_counter_system.py --list
python hp_scanner_counter_system.py --get
python hp_scanner_counter_system.py --set 1000

# GUI интерфейс для Windows
run_scanner_counter_system.bat
```

### 🌟 Улучшенный способ (с pyusb для точного чтения)
```bash
# Улучшенная версия - исправляет проблему с ответами
python hp_scanner_counter_improved.py --scan
python hp_scanner_counter_improved.py --get
python hp_scanner_counter_improved.py --set 1000

# Автоматическое определение подключения
python hp_scanner_counter_auto.py --scan
python hp_scanner_counter_auto.py --get
```

### 🔌 USB подключение
```bash
# Показать USB принтеры
python hp_scanner_counter_usb.py --list

# Основные команды
python hp_scanner_counter_usb.py --get
python hp_scanner_counter_usb.py --set 1000
python hp_scanner_counter_usb.py --reset
```

### 🌐 Сетевое подключение
```bash
# Основные команды
python hp_scanner_counter.py 192.168.1.100 --get
python hp_scanner_counter.py 192.168.1.100 --set 1000
python hp_scanner_counter.py 192.168.1.100 --reset
```

### 📊 Примеры команд

#### Получить текущее значение счетчика
```bash
python hp_scanner_counter_auto.py --get
python hp_scanner_counter_usb.py --get
python hp_scanner_counter.py 192.168.1.100 --get
```

#### Установить значение счетчика на 1000
```bash
python hp_scanner_counter_auto.py --set 1000
python hp_scanner_counter_usb.py --set 1000
python hp_scanner_counter.py 192.168.1.100 --set 1000
```

#### Сбросить счетчик в 0
```bash
python hp_scanner_counter_auto.py --reset
python hp_scanner_counter_usb.py --reset
python hp_scanner_counter.py 192.168.1.100 --reset
```

## 📖 Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `ip` | IP адрес принтера | Обязательный |
| `--port` | Порт подключения | 9100 |
| `--timeout` | Таймаут подключения (сек) | 10 |
| `--get` | Получить текущий счетчик | - |
| `--set COUNT` | Установить счетчик на COUNT | - |
| `--reset` | Сбросить счетчик в 0 | - |
| `--info` | Информация о принтере | - |

## 🔍 Справка

Получить полную справку по использованию:
```bash
python hp_scanner_counter.py --help
```

## ⚙️ Технические детали

### PJL команды

Скрипт использует следующие PJL команды для работы со счетчиком:

**Получение счетчика:**
- `@PJL INQUIRE SCANCOUNT`
- `@PJL INQUIRE SCANCOUNTER`
- `@PJL INQUIRE SCANPAGES`
- `@PJL INFO SCANCOUNT`
- `@PJL INFO SCANCOUNTER`

**Установка счетчика:**
- `@PJL SET SCANCOUNT=<значение>`
- `@PJL SET SCANCOUNTER=<значение>`
- `@PJL SET SCANPAGES=<значение>`
- `@PJL DEFAULT SCANCOUNT=<значение>`
- `@PJL DEFAULT SCANCOUNTER=<значение>`

### Сетевое подключение

- **Протокол**: TCP
- **Порт**: 9100 (стандартный для HP принтеров)
- **Кодировка**: ASCII
- **Таймаут**: 10 секунд (настраивается)

## 🛠️ Устранение неполадок

### ❌ Проблема: Скрипт возвращает "Command sent" вместо числа

**Решение:** Используйте **улучшенную версию**:
```bash
# Вместо старых версий используйте:
python hp_scanner_counter_improved.py --get

# Или запустите тест исправления:
test_counter_fix.bat
```

**Причины проблемы:**
- Системные команды (Windows `copy`, Linux `lp`) могут только отправлять данные
- Ограниченное чтение ответов через USB без pyusb
- Неполная двунаправленная связь с принтером

**Что исправлено в новой версии:**
- ✅ Улучшенный парсинг ответов PJL команд
- ✅ Альтернативные методы получения счетчика
- ✅ Кэширование значений для системных методов
- ✅ Автоматический выбор лучшего метода подключения

### 🔌 Принтер не отвечает
- Проверьте IP адрес принтера или USB подключение
- Убедитесь, что принтер включен и подключен
- Проверьте, что порт 9100 не заблокирован файрволом
- Попробуйте увеличить таймаут с помощью `--timeout`
- **Для USB:** Запустите от администратора и установите `pyusb`

### ⚙️ Не удается изменить счетчик
- Некоторые модели принтеров могут не поддерживать изменение счетчика
- Попробуйте перезагрузить принтер после изменения
- Убедитесь, что принтер не используется другими процессами
- **Для USB:** Используйте `hp_scanner_counter_improved.py`

### 📋 Неполная информация о принтере
- Не все принтеры поддерживают все PJL команды
- Скрипт автоматически пробует различные варианты команд
- Используйте диагностические скрипты для проверки совместимости

## 🔒 Безопасность

- Скрипт подключается к принтеру только для отправки PJL команд
- Не сохраняет никаких данных о принтере
- Не изменяет настройки печати или других функций принтера

## 📝 Логирование

Скрипт выводит подробную информацию о выполняемых операциях:
- ✓ Успешные операции отмечены зеленой галочкой
- ✗ Ошибки отмечены красным крестиком  
- ⚠ Предупреждения отмечены желтым треугольником
- → Отправленные команды
- ← Ответы принтера

## 🤝 Совместимость

Скрипт протестирован с:
- HP LaserJet Pro 400 Series
- HP LaserJet Pro 400 M401
- HP LaserJet Pro 400 M425 (MFP)

Может работать с другими принтерами HP, поддерживающими PJL.

## 📄 Лицензия

Этот скрипт предоставляется "как есть" без каких-либо гарантий. Используйте на свой страх и риск.
